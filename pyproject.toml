[project]
name = "acmi-maid"
version = "0.1.0"
description = "Pure-Python ACMI 2.2 toolset for Tacview flight recordings"
authors = [
    { name = "liuxiujing", email = "liuxj@baiyangai.com" }
]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.8,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
